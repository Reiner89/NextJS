import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pricing Page",
  description: "Page of Pricing",
  keywords: ["pricing page", "sale", "off"],
};

export default function PricingPage() {
  return (
    <div>
      <h1>Pricing</h1>
    </div>
  );
}
